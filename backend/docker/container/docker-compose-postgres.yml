services:
  postgres:
    image: postgres:11.5
    container_name: keep_postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: admin
      POSTGRES_USER: admin
      POSTGRES_DB: keep-db
